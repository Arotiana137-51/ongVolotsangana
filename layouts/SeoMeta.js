// Deprecated: kept as a no-op for backward compatibility.
// SEO is now handled via Next.js App Router `generateMetadata` in each route,
// using the helper at `lib/seo/getMetadata.js`.
const SeoMeta = () => null;
export default SeoMeta;
